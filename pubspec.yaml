name: novasignal
description: Sua plataforma de trading inteligente
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Ícones do app
  cupertino_icons: any

  # WebSocket para Deriv API
  web_socket_channel: any

  # Deep Links para Android
  uni_links: ^0.5.1

  # URL Launcher para OAuth
  url_launcher: any

  # Storage local
  shared_preferences: any

  # Firebase (opcional, para autenticação e database)
    firebase_core: any
    firebase_auth: any
    cloud_firestore: any

  # HTTP client
  http: any

  # State management (opcional)
  provider: any

  # Biometria
  local_auth: any

  # JSON
  json_annotation: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  json_serializable: ^6.7.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true

  # Assets
  # assets:
  #   - images/
  #   - icons/

  # Fonts personalizadas (opcional)
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
  #       - asset: fonts/CustomFont-Bold.ttf
  #         weight: 700